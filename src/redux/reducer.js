const initState = {
  houseListings: [
    {id:1, name: 'home', adress: 'test1', city: 'provo', zip:'test'},
    {id:2, name: 'home2', adress: 'test2', city: 'provo', zip:'test'}
  ]
}

const reducer = (state = initState, actions) => {
    switch(actions.type){
       
          
        
          
        default: return state
    }

}

export default reducer;